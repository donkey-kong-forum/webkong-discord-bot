# Copy to .env for local runs. On Railway, set these in the service's Variables tab.

# Required: the bot token from the Discord Developer Portal (Bot tab).
DISCORD_TOKEN=

# Optional overrides (defaults shown).
# WEBKONG_BASE=https://greentie.dev/webkong
# ROTATE_INTERVAL_MS=45000
# PORT=3000
